# env files contain secrets — never commit
.env
.env.local
.env.*.local
server/.env

# Live admin-editable data + usage stats (seeded/created on the VPS, not in git)
content.json
tracks.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.DS_Store
.idea/
.vscode/

# One-off generated logo exports (not part of the app)
logo-*.png

# Working artifacts (Word docs for the client, etc.)
*.docx
